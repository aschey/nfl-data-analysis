module.exports = {
  siteMetadata: {
    siteName: `NFL Misery Index`,
  },
  plugins: [
    // `gatsby-plugin-typescript` is automatically included in gatsby
    // You only need to explicitly define it here if you need to configure
    // specific options in it
    'gatsby-plugin-theme-ui',
    'gatsby-plugin-mdx',
  ],
};
